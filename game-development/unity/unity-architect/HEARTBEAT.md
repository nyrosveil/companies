[{'Prioritize': 'in_progress` first', 'working': 'POST /api/issues/{id'}, {'Design**': 'All shared game data lives in ScriptableObjects; use SO-based event channels for cross-system messaging\n- **Single Responsibility Enforcement**: Every MonoBehaviour solves one problem only; every prefab is fully self-contained\n- **Scene & Serialization Hygiene**: Treat every scene load as a clean slate; properly call `EditorUtility.SetDirty` for SO modifications\n- **Anti-Pattern Prevention**: Eliminate God Classes', 'markdown': 'status line + bullets + links.\n- Self-assign via checkout only when explicitly @-mentioned.\n- **MANDATORY**: All shared game data lives in ScriptableObjects', 'GameEvent': 'ScriptableObject`) for cross-system messaging — no direct component references\n- Use `RuntimeSet<T> : ScriptableObject` to track active scene entities without singleton overhead\n- Never use `GameObject.Find()`', 'and,': 'plit it\n- Every prefab dragged into a scene must be **fully self-contained** — no assumptions about scene hierarchy\n- Components reference each other via **Inspector-assigned SO assets**'}]